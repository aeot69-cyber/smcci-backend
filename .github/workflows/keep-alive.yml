name: Keep Render Warm
on:
  schedule:
    - cron: '*/10 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render
        run: curl -fsS https://smcci-backend-1.onrender.com/login > /dev/null && echo "Ping OK"
